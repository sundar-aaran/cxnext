# Application
APP_NAME=codexsun

# Core host identity, listening endpoints, TLS, and filesystem roots.
APP_ENV=development
APP_TYPE=200
APP_CLIENT=100
APP_HOST=0.0.0.0
APP_DOMAIN=api.codexsun.local
APP_HTTP_PORT=4000
APP_HTTPS_PORT=4003
CLOUDFLARE_ENABLED=false
TLS_ENABLED=false

# Frontend
FRONTEND_DOMAIN=app.cxnext.local
FRONTEND_HTTP_PORT=3000
FRONTEND_HTTPS_PORT=3001

# Backend
DESKTOP_READY_TIMEOUT_MS=30000
DEV_READY_TIMEOUT_MS=60000

# Database
DB_DRIVER=mariadb
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cxnext_db
DB_USER=root
DB_PASSWORD=change-me
DB_SSL=false

# Security
JWT_SECRET=replace-with-a-long-random-development-secret
JWT_EXPIRES_IN_SECONDS=28800
AUTH_TOKEN_EXPIRES_SECONDS=28800
AUTH_DEFAULT_ADMIN_PASSWORD=Admin@12345
AUTH_MAX_LOGIN_ATTEMPTS=5
AUTH_LOCKOUT_MINUTES=30
ADMIN_SESSION_IDLE_MINUTES=30
SECRET_ROTATION_DAYS=90

# Notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=user@example.com
SMTP_PASS=change-me
SMTP_FROM_EMAIL=user@example.com
SMTP_FROM_NAME="CX Next"

# Razorpay
RAZORPAY_ENABLED=false
RAZORPAY_KEY_ID=change-me
RAZORPAY_KEY_SECRET=change-me
