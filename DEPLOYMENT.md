# cxnext Docker Deployment

Container deployment files now live in `.container`.

Use [.container/usage.md](.container/usage.md) for the complete Ubuntu build, MariaDB, app start, and update instructions.
